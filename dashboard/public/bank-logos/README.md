# Bank logos (Vite static assets)

Same rules as `public/bank-logos/README.md` at the Laravel root.

Place owner-supplied files here as `{bankId}.svg` (or `.png` / `.webp`).  
They are served at `/bank-logos/{bankId}.ext` during dashboard dev/build.

Do not commit logos without owner approval.
